// using .ajax with jQuery returns a Promise object
// This is just an object that is waiting for some data to 'resolve'

// We can (But never will) create new promises like this:

// It will pass that data into the callback function in `then`
